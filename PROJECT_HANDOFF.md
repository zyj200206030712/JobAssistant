# 个人求职工作台：新对话交接文档

> 最后整理：2026-08-17（Asia/Shanghai）  
> 项目目录：当前 `README.md` 所在目录  
> 新对话开始时，请让开发助手先完整阅读本文件、项目根目录的 `AGENTS.md` 和 `README.md`，再检查实际代码。以当前文件内容为最终事实，不要凭本文件猜测未验证的实现。

## 1. 项目目标与硬性约束

这是仅供个人使用、双击 `index.html` 即可运行的离线求职管理工作台，用于企业投递管理、学习资料、算法题、模拟面试、招聘网站和常用链接管理。

必须保持：

- 仅使用 HTML、CSS、原生 JavaScript 和 JSON；
- 中文界面，桌面端优先，简洁、专业、适合长期使用；
- 不使用 React、Vue、Angular、Node.js 运行环境、Python Web 框架、后端接口或数据库；
- 不增加登录、注册、多用户权限、云端服务或部署流程；
- 所有功能必须真实可用，表单有校验，删除有确认，操作有提示，空数据有说明；
- 项目必须离线运行，不要求安装任何运行环境；
- 后续修改应小步进行，说明修改文件、实现内容和测试方式；
- 编辑代码使用安全的局部修改，不能覆盖或删除用户的 JSON 备份。

## 2. 项目结构与启动方式

```text
JobAssistant/
├── index.html
├── style.css
├── script.js
├── data/
│   ├── companies.json
│   ├── learning.json
│   ├── websites.json
│   └── quicklinks.json
├── assets/
│   └── icons/
├── README.md
└── PROJECT_HANDOFF.md
```

启动方式：直接双击 `index.html`。普通功能可使用主流浏览器；文件绑定和“另存为”优先使用最新版 Chrome 或 Edge。

## 3. 当前已实现功能

### 首页

- 企业总数、已投递、待投递、面试、Offer 统计；
- 最近添加企业和近期事项；
- 邮箱、AI 工具等常用链接的新增、编辑、删除和新标签页打开。

### 企业管理

- 企业新增、编辑、删除、分类切换、批次筛选、状态筛选；
- 企业名称是唯一必填项，其余企业及投递字段均为选填；
- 投递状态增加“不感兴趣”，用于保留已了解但明确不投递的企业；该状态可搜索、筛选，但不计入求职进度且不生成截止或投递待办；
- 企业列表使用备注列替代截止日期列，直接显示最多三行备注内容并保留手动换行；优先使用投递备注，其次使用企业备注，悬停可查看全文；截止日期字段和首页截止提醒保持不变；
- 企业管理工作区采用左侧六列紧凑列表和右侧完整详情栏；点击或键盘选择企业后显示全部字段并高亮当前行，窄屏时详情栏移到列表上方；选择状态仅存在于运行时，不写入企业数据；
- 企业紧凑列表的目标岗位列第二行显示地点，当前状态列第二行显示批次；方向只在右侧完整详情中显示；
- 企业列表显示最近修改日期和时间；每次新增或编辑保存时更新 `updatedAt`，旧记录缺少该字段时提示编辑后生成；
- 企业字段包括：名称、分类、企业信息来源/官网、招聘网址、地点、企业备注、岗位、方向、批次、状态、投递日期、截止日期、岗位/投递状态链接和投递备注；
- 搜索覆盖上述全部用户可编辑字段，支持中文、英文大小写不敏感的包含匹配、日期片段和网址域名；不搜索 `id`、`createdAt`、`updatedAt`；
- 招聘链接在新标签页打开，优先顺序为岗位链接、招聘网址、企业信息来源/官网；
- 日期使用原生日期框，年份限制四位；截止日期不能早于投递日期；
- 截止日期过去后不会删除、隐藏或清空企业及链接，并会退出首页“近期截止事项”；截止日期仍可在企业编辑表单中查看和修改；
- 删除企业只会在用户点击删除并二次确认后发生。

当前四种企业分类：

- `央国企`
- `研究所`
- `私企`
- `外企`

旧数据和旧备份迁移规则：

- 原分类为“研究所/央国企”，名称含“研究所、研究院、科学院、院所”时迁移为“研究所”；
- 其余原“研究所/央国企”迁移为“央国企”；
- 原“私企”的企业备注包含精确词语“外企”时迁移为“外企”；
- 其他私企保持不变；
- 迁移只改变分类，不修改链接、备注、岗位、状态和时间。

### 学习中心

- 我的项目及八股、央国企行测、贵州选调申论、LeetCode、模拟面试五个模块；
- 行测题目的答案编辑框独占整行、默认显示八行并支持继续纵向拖动；
- 项目字段已调整为项目名称、项目时间、项目简介、技术栈、主要工作、项目困难及解决、项目成果、面试讲解；主要工作独占整行；
- 八股问答的标准答案文本框默认显示十行并支持继续纵向拖动，便于编写长答案；
- 六类学习记录均包含非负整数 `encounterCount`；卡片右上角标志显示次数并提供 `-1`/`+1`，调整后写入本机数据但不改动 `updatedAt`；旧记录和旧备份缺少该字段时归一化为 `0`；
- 项目、八股、行测、申论、LeetCode、模拟面试卡片均支持点击打开大尺寸详情；相关操作按钮不会误触详情；
- LeetCode 的名称和题目详情为必填项，题号、原题链接、难度和类型为选填项；未填写题号时卡片标题不显示编号占位，详情显示“暂无编号”；另包含思路、Python 代码、复杂度、完成和复习状态，并支持旧数据缺少 `problemDetail`；
- 模拟面试当前只保留“问题”和“回答”，支持详情和随机抽题；旧记录会迁移回答字段；
- 六类学习记录均支持粘贴、拖入和选择图片，图片作为记录附件，不进行正文内图文混排；
- 图片支持 PNG、JPEG、WebP，单张原图最大 10 MB，每条记录最多20张，最长边压缩到1920像素；
- 详情页包含图片画廊和大图预览；大图使用 `object-fit: contain` 完整显示竖图、横图和方图，缩略图保持裁剪；
- 图片大图支持遮罩、Esc、上一张和下一张。
- 启动时仅在成功读取到结构完整、记录 ID 与图片 ID 有效的本机学习数据后清理无主图片；学习数据缺失、损坏或只有部分模块时保留 IndexedDB 图片，避免恢复前误删；
- 合并不含 `images` 字段的旧版学习记录时，相同 ID 的现有记录保留当前图片；显式空数组或覆盖导入仍以导入内容为准。

### 招聘网站与设置

- 招聘网站支持卡片、新增、编辑、删除、搜索、分类和新标签页打开；
- 支持浅色/深色模式；
- 支持完整 JSON 导入、合并或覆盖导入、完整 JSON 导出；
- 设置页显示文字和图片存储统计。

## 4. 数据存储架构（非常重要）

项目没有后端。`data/*.json` 是初始/示例数据，不是浏览器运行时的实时数据文件。

运行时数据位置：

- 企业：`localStorage` 键 `jobAssistant.companies`；
- 学习资料：`localStorage` 键 `jobAssistant.learning`；
- 招聘网站：`localStorage` 键 `jobAssistant.websites`；
- 首页常用链接：`localStorage` 键 `jobAssistant.quickLinks`；
- 主题和同步偏好：其他 `jobAssistant.*` 键；
- 图片二进制：IndexedDB 数据库 `JobAssistantMedia` 的 `images` 对象仓库；
- 本地同步文件句柄：同一 IndexedDB 的 `fileHandles` 对象仓库。

清除“Cookie 和其他站点数据”可能同时删除文字数据、图片库和文件绑定。只清理普通缓存图片通常不等同于清除站点数据。使用 InPrivate、切换浏览器配置文件、移动 `index.html` 路径也可能形成不同的浏览器存储环境。

## 5. JSON 备份、绑定和同步的安全语义

### 手动导出

- “选择位置并导出”使用 `showSaveFilePicker` 弹出系统“另存为”窗口；
- 用户可手动选择项目的 `JobAssistant` 文件夹；浏览器安全策略不允许网页未经确认直接指定任意本地目录；
- 默认文件名包含日期和时间，例如 `JobAssistant-backup-20260817-174426.json`，避免同一天多次导出互相覆盖；
- 完整备份把图片转换为 Data URL 写进 JSON；运行时记录只保留图片元数据；
- 不支持文件选择器的浏览器回退到默认下载目录。

### 绑定本地备份文件

绑定现在是纯选择操作：

- 使用 `showOpenFilePicker` 选择已有 JSON；
- 只保存文件句柄，不读取文件内容，也不写入或覆盖文件；
- 重新绑定会关闭自动同步并清空“最近同步”显示；
- 从文件恢复网页数据必须使用“导入 JSON”，不能使用绑定或立即同步。

### 写入绑定文件

- “立即同步（写入文件）”才会把网页当前数据写入绑定文件；
- 写入前明确提示该操作会覆盖文件，用户确认后才执行；
- 开启“修改后自动同步”也会提示覆盖含义并要求确认；
- 浏览器重开后可能保留文件句柄但收回写权限，此时无需重新绑定，点击“立即同步（写入文件）”重新授权即可；
- 如果显示“尚未绑定”，说明 IndexedDB 文件句柄已丢失，常见原因是站点数据被清除或使用了不同浏览器环境。

## 6. 备份安全基线

文件绑定、导入和同步必须始终保持三种不同语义：绑定只记录文件句柄，导入从文件恢复网页数据，同步才会把网页数据写入文件。曾经存在过“绑定后立即写入”的错误实现，现已修复：

- 绑定不再调用同步；
- 绑定改用打开文件选择器；
- 重新绑定自动关闭自动同步；
- 手动写入和开启自动同步均增加清晰确认；
- 手动导出改用另存为窗口并使用日期时间文件名。

后续绝不能恢复“绑定后立即写入”的旧行为，也不能把“绑定”解释为“导入”。任何个人备份文件都不得提交到公开代码仓库；需要远程保存时，应使用独立私有仓库或其他受控存储，并在复制前后核对文件哈希。

## 8. 关键代码入口

主要逻辑集中在 `script.js`：

- `loadCompanies()`：加载并迁移浏览器企业数据；
- `resolveCompanyCategory()`、`migrateLegacyCompanyCategories()`：四分类迁移；
- `getFilteredCompanies()`：企业全字段搜索及筛选；
- `createTodos()`：首页截止提醒，不删除数据；
- `bindLocalBackupFile()`：纯绑定，不读写；
- `syncToBoundFile()`：确认后写入绑定文件；
- `exportAllData()`、`createCompleteBackupPayload()`：完整备份及图片导出；
- `prepareImportData()`、`normalizeImportedCompanies()`：导入验证和旧分类兼容；
- IndexedDB 图片读写函数位于 `openImageDatabase()` 附近。

界面结构位于 `index.html`，视觉样式和深色模式位于 `style.css`。详细用户说明见 `README.md`。

## 9. 开发和验证注意事项

- 修改前先搜索相关 DOM ID、数据字段、导入校验、回填逻辑和 README，避免只改一半；
- 保持旧 JSON 兼容，新增字段需要默认值或迁移；
- 不要修改现有备份文件来做测试；测试应使用副本；
- 涉及图片时验证 IndexedDB、导出 Data URL、导入还原和失败回滚；
- 涉及文件系统时明确区分“导入（文件到网页）”与“同步（网页到文件）”；
- 所有会覆盖文件的动作必须有明确文案和用户确认；
- 截止日期永远不能触发企业自动删除或链接清理；
- 企业分类只允许央国企、研究所、私企、外企，但导入仍需兼容旧“研究所/央国企”；
- JavaScript 可用 Node 做开发期语法检查，但项目运行不得依赖 Node。Windows PowerShell 管道需要显式 UTF-8：

```powershell
$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new()
Get-Content -LiteralPath .\JobAssistant\script.js -Raw -Encoding UTF8 | node --check -
```

最终应至少手动验证：双击启动、导航、企业增删改查、四分类迁移、学习详情、图片预览、完整导入导出、绑定不写入、确认后同步和深色模式。

## 10. 新对话建议开场语

在新对话中可以直接发送：

> 请先完整阅读项目根目录的 `PROJECT_HANDOFF.md`、工作区 `AGENTS.md` 和项目 `README.md`，再检查当前代码。继续开发时严格保持纯 HTML/CSS/JavaScript/JSON、双击离线运行和备份安全约束；不要覆盖任何 `JobAssistant-backup*.json`。
